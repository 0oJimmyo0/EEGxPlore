#!/usr/bin/env bash

# Shared ICASSP pilot settings. Override paths/resources in the environment;
# method-specific settings belong only in run_pilot.sh.
ICASSP_REPO_DIR="${ICASSP_REPO_DIR:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)}"
FOUNDATION_DIR="${FOUNDATION_DIR:-/data/neurogroup/mingyangjiang/data/weights/pretrained_weights.pth}"
SEEDV_DATA_DIR="${SEEDV_DATA_DIR:-/data/neurogroup/mingyangjiang/data/SEED-V_processed_lmdb}"
FACED_DATA_DIR="${FACED_DATA_DIR:-/data/neurogroup/mingyangjiang/data/FACED}"
FACED_META_CSV="${FACED_META_CSV:-/data/neurogroup/mingyangjiang/data/metadata/Recording_info.csv}"
ISRUC_DATA_DIR="${ISRUC_DATA_DIR:-/data/neurogroup/mingyangjiang/data/ISRUC}"
PHYSIONET_DATA_DIR="${PHYSIONET_DATA_DIR:-/data/neurogroup/mingyangjiang/data/PHYSIO_MI}"
MODEL_ROOT="${MODEL_ROOT:-$ICASSP_REPO_DIR/output/icassp2027_depth/health20}"
CUDA_ID="${CUDA_ID:-0}"
SEED="${SEED:-42}"
EPOCHS="${EPOCHS:-20}"
BATCH_SIZE="${BATCH_SIZE:-32}"
NUM_WORKERS="${NUM_WORKERS:-0}"
LR="${LR:-1e-4}"
MIN_LR="${MIN_LR:-5e-6}"
WEIGHT_DECAY="${WEIGHT_DECAY:-5e-2}"
DROPOUT="${DROPOUT:-0.1}"
INPUT_SCALE_DIVISOR="${INPUT_SCALE_DIVISOR:-100.0}"
WARMUP_EPOCHS="${WARMUP_EPOCHS:-0}"
WARMUP_START_FACTOR="${WARMUP_START_FACTOR:-0.002}"
LABEL_SMOOTHING="${LABEL_SMOOTHING:-0.1}"
CLASS_WEIGHT_MODE="${CLASS_WEIGHT_MODE:-none}"
USE_COMPONENT_LR="${USE_COMPONENT_LR:-1}"
LR_BACKBONE_MULT="${LR_BACKBONE_MULT:-0.5}"
LR_ROUTER_MULT="${LR_ROUTER_MULT:-2.0}"
LR_EXPERT_MULT="${LR_EXPERT_MULT:-1.5}"
LR_CLASSIFIER_MULT="${LR_CLASSIFIER_MULT:-3.5}"
LR_OTHER_MULT="${LR_OTHER_MULT:-1.0}"
LR_DEPTH_MULT="${LR_DEPTH_MULT:-1.0}"
SELECTED_CHECKPOINT_DIAGNOSTICS="${SELECTED_CHECKPOINT_DIAGNOSTICS:-1}"
# Nonzero expert-init noise is reserved for explicitly labeled routing
# diagnostics; the primary ICASSP configuration remains exactly zero.
MOE_EXPERT_INIT_NOISE_STD="${MOE_EXPERT_INIT_NOISE_STD:-0.0}"
ICASSP_ROUTING_DIAGNOSTIC="${ICASSP_ROUTING_DIAGNOSTIC:-0}"
